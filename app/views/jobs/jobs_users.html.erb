<div class="content-wrapper">
  <div class="container-xxl flex-grow-1 container-p-y">
    <div class="card">
      <h5 class="card-header">Users</h5>
      <div class="table-responsive text-nowrap">
      <%= link_to 'Job Listing', jobs_path, class:"btn btn-primary mb-3 project-btn"%>
        <table class="table" id="jobs-users-list" data-source="<%= fetch_jobs_users_path(format: :json, jobid: @jobid) %>">
          <caption class="ms-4">
            List of Job Users
          </caption>
        </table>
      </div>
    </div>
  </div>
</div>
